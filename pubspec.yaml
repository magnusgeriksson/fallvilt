name: fallvilt
description: Registrering av fallvilt.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  bloc: ^7.1.0
  flutter_bloc: ^7.2.0
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  formz: ^0.4.0
  uuid: ^3.0.4
  moor: ^4.5.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.0.0
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  bloc_test: ^8.1.0
  moor_generator: ^4.5.1
  build_runner: ^2.1.1
flutter:
  uses-material-design: true
